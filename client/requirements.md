## Packages
framer-motion | Smooth animations for entry, hover, and layout transitions
react-parallax-tilt | 3D tilt effect for the profile card
lucide-react | Beautiful icons for social links and UI elements
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
- Background video expects file at /assets/background.mp4 (mapped to client/public/assets/background.mp4)
- Lanyard API used for Discord status (WebSocket + REST)
- Theme colors are dynamic based on API response
